[package]
name = "build-script-network"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[build-dependencies]
reqwest = { version = "0.11", features = ["blocking"] }
ureq = "2.9"
curl = "0.4"
hyper = "0.14"

[dependencies]
